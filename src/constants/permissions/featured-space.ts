const create = 'featuredSpace.create';
const update = 'featuredSpace.update';
const view = 'featuredSpace.view';
const list = 'featuredSpace.list';
const remove = 'featuredSpace.remove';

export const featuredSpace = {
  create,
  update,
  view,
  list,
  remove,
};
